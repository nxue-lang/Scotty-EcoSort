Waste Sorting
=============

Open `index.html` in a browser to play offline.

Files:
- `index.html` builds the page.
- `style.css` controls the layout.
- `game.js` contains the game rules and drawing code.

Move with W A S D or the arrow keys. Catch each falling item, then carry it to
the matching bin. Press R to restart after game over.
